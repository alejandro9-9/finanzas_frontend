# Desarrollo local: el navegador llama directamente al backend.
NEXT_PUBLIC_API_URL=http://localhost:5260

# Vercel: configura esta variable con la URL HTTPS de Render y no configures
# NEXT_PUBLIC_API_URL para que el navegador use el proxy del mismo dominio.
BACKEND_API_URL=http://localhost:5260
